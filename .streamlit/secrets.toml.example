# Streamlit Community Cloud → app → Settings → Secrets : paste the lines below
# (locally you can instead copy this to .streamlit/secrets.toml, or use .env)
# This file is a TEMPLATE — never put a real key in a committed file.

OPENAI_API_KEY = "sk-...your-valid-key..."
OPENAI_MODEL = "gpt-4o-mini"
OPENAI_EMBED_MODEL = "text-embedding-3-small"
